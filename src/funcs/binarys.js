/* 
function GetMidleStartEndArray(array) {
    start = 0
    end = array.lenght - 1
    const __midleArray__ = Math.floor((start + end) / 2)

    return {
        "start": start,
        "end": end,
        "midle": __midleArray__
    }
} */